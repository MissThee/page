import request from 'src/docviewer/utils/request';
import Global from 'src/docviewer/utils/global';

export default {
  // getRepositoryInfo: requestData => request({
  //   method: 'get',
  //   url: Global.GITHUB_API_HOST + '/repos/' + Global.USER + '/' + Global.DEFAULT_REPOSITORY
  // }),
};
